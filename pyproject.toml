[project]
name = "snowalerts-tc-discord"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aws-lambda-powertools>=3.23.0",
    "discord-interactions>=0.4.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.9",
]
